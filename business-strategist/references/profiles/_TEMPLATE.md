# Strategist Profile — <Your Business Name>

> This is a SCAFFOLD. It is never used as real data — the `<angle-bracket>` prompts are instructions, not values.
> The agent builds a real copy of this WITH you on first run (onboarding scans your workspace first, then confirms).
> To fill it in by hand instead: copy this file to `<your-business>.md` (no leading underscore) and replace every
> `<...>`. A domain you don't track yet is recorded as `not tracked` — that's a legitimate value, not a failure.

## Source map (what each analyst reads — and NOTHING else)

> One block per domain. Every source listed here was probed with ONE real read before being saved —
> if it can't be read today, it can't be trusted at review time. Exact paths and read patterns, never
> "check the books somewhere."

- **Money:** <path(s) + where the numbers live, e.g. "books at finances/2026-pnl.xlsx — monthly tabs" and/or "pulse/pulse-log.md — MRR column"> | not tracked
- **Leads & Sales:** <e.g. "leads/ledger.md — full read" · "sales/pipeline.md + sales/deals/*.md"> | not tracked
- **Clients & Delivery:** <e.g. "clients/*.md — notes + status lines" · "churn-tracker.md"> | not tracked
- **Audience & Marketing:** <e.g. "pulse/pulse-log.md — list-size column" · "content/published/ — file dates as publishing record"> | not tracked
- **Goals & Execution:** <path to goals/tasks file + where the current goals live in it, e.g. "TASKS.md — the 'This Week' section"> | "goals captured in profile below"

## Goals (only if no goals file exists — otherwise the file above is the source of truth)
- <current top 1–3 goals, stated scoreably, with the date they were set>

## Review rhythm
- **Cadence:** <quarterly | monthly | on demand> — **Next review due:** <date>
- **Scheduled task:** <"created YYYY-MM-DD" | "manual — I say 'run my review'">

## Language & identity
- **Business name:** <name>
- **My people are called:** <members | clients | customers>
- **Brand/voice skill or file:** <name/path | "none">

## Notes
- <anything the strategist should always remember — seasonality, a pivot date that makes older data
  incomparable, a domain deliberately not tracked and why, runner-up sources not yet mapped, etc.>
