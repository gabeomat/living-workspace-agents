# Nurture Profile — <Your Business Name>

> This is a SCAFFOLD. It is never used as real data — the `<angle-bracket>` prompts are instructions, not values.
> The agent builds a real copy of this WITH you on first run (onboarding). To fill it in by hand instead: copy this
> file to `<your-business>.md` (no leading underscore) and replace every `<...>`. Delete any line you don't use.

## Offers & prices (sequences sell ONLY these)
- **<Offer 1 name>:** <one-line promise — exact price — buy path (checkout link / application / booking)>
- **<Offer 2 name>:** <same>
- **Offer ladder:** <what naturally follows each purchase | "flat — one offer">

## The list
- **Platform:** <MailerLite | Kit | Mailchimp | GoHighLevel | ActiveCampaign | …>
- **Connected to Claude:** <yes, via <connector/MCP> | no — deliver handoff files>
- **List state:** <rough size — engagement level — last emailed <when>>
- **Arrival points:** <opt-in pages, events, purchases — where new subscribers come from>

## Sender & compliance
- **From-name:** <what subscribers see>
- **Footer:** <business name, address, unsubscribe — what the platform appends or needs included>

## Voice
- **Brand-voice skill:** <name of installed skill | "none — use the samples below">
- **Sounds-like-me samples:** <2–3 file paths or pasted emails I actually sent and liked>
- **Voice rules:** <sign-off, phrases I love, things I'd never say>
- **Cadence comfort:** <e.g. "max 1/day even during a close" | "daily is fine during launches">

## Story bank
- **Location:** `nurture/stories/story-bank.md` <(seeded on first run — add stories anytime: "add a story")>

## Notes
- <anything the agent should always remember when writing to my list>
